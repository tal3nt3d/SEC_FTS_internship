[pytest]
testpaths = app/tests

addopts =
    -v
    --tb=short

markers =
    repository: Tests for repository layer
    service: Tests for service layer
    api: Tests for API endpoints
    validation: Tests for validation errors
    history: Tests for task history
    export: Tests for CSV export
    auth: Tests for authentication